[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
addopts = -v --tb=short
# Filter warnings for dataclasses that start with "Test" (domain models, not test classes)
filterwarnings =
    ignore::pytest.PytestCollectionWarning
markers =
    requires_stata: mark test as requiring Stata installation
